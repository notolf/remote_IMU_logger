; =============================================================================
;  PlatformIO project — M5Stack CoreS3 Static Level Logger
; -----------------------------------------------------------------------------
;  Build:   pio run
;  Flash:   pio run -t upload
;  Monitor: pio device monitor   (115200 baud)
; =============================================================================
[platformio]
default_envs = m5stack-cores3

[env:m5stack-cores3]
; -----------------------------------------------------------------------------
; Platform / board / framework
; -----------------------------------------------------------------------------
;  Official Espressif PlatformIO platform, which ships the ESP32-S3 Arduino core
;  and the `m5stack-cores3` board definition. If your installed PlatformIO is
;  older and cannot find the board, switch to the community "pioarduino" platform
;  by replacing the line below with (see README.md):
;
;    platform = https://github.com/pioarduino/platform-espressif32/releases/download/53.03.13/platform-espressif32.zip
;
platform  = espressif32
board     = m5stack-cores3
framework = arduino

; -----------------------------------------------------------------------------
; Serial
; -----------------------------------------------------------------------------
monitor_speed   = 115200
upload_speed    = 1500000
monitor_filters = esp32_exception_decoder, time

; -----------------------------------------------------------------------------
; Libraries
; -----------------------------------------------------------------------------
;  This is a standalone, offline build — no WiFi/web stack.
;  * M5Unified — board/IMU(BMI270)/RTC/PMU/display/touch abstraction
;  SD / FS / SPI / Preferences / time are built into the ESP32 Arduino core.
;
;  M5GFX is a dependency of M5Unified. We pull it explicitly from GitHub because
;  the PlatformIO *registry* tarball for 0.2.22 is incomplete — it is missing
;  headers such as lgfx/internal/memory.h and lgfx/v1/misc/DataWrapper.hpp, which
;  breaks compilation with "No such file or directory". The GitHub tag is the
;  complete source and still satisfies M5Unified's ">=0.2.22" requirement.
;  (If a newer registry M5GFX is published and works, you can delete the git line
;  and let M5Unified pull M5GFX itself.)
lib_deps =
    m5stack/M5Unified @ ^0.2.0
    https://github.com/m5stack/M5GFX.git#0.2.22

; -----------------------------------------------------------------------------
; Build flags
; -----------------------------------------------------------------------------
;  CoreS3 has 8 MB PSRAM (used for the full-screen display sprite).
;  USB CDC on boot routes Serial to the USB-C port.
build_flags =
    -DBOARD_HAS_PSRAM
    -DARDUINO_USB_CDC_ON_BOOT=1
    -DCORE_DEBUG_LEVEL=1

; -----------------------------------------------------------------------------
; Flash / partitions  (16 MB flash on the CoreS3)
; -----------------------------------------------------------------------------
board_build.flash_size = 16MB
board_build.partitions = default_16MB.csv
